<!DOCTYPE html>
<html lang="en">
<head>
    <head>
        <%- include('./partials/head.ejs', {title: 'Show Page'}) %>
    </head>
    
</head>
<body>
    <nav>
        <%- include('./partials/nav.ejs') %>
      </nav>
    <h1>Fruits Show Page</h1>
    <p>
    <% if(fruit){ %>

      The Item is <%= fruit.name %> and  <%= fruit.color %> 

        <% if(fruit.readyToEat === true) { %>
            <span>It is ready to eat</span>
        <% } else { %>
            <span>It is not ready to eat</span>
            <% }%>
            
    <% }else { %>
        
        <h1>Fruit at that index does not exist</h1>

    <% } %>
</p>
<div class="showPageContainer">
    <a href="/main/<%= main._id %>/edit">Edit this Entry</a>
    <a href="/">Home</a>
</div>


    <%- include('./partials/footer.ejs') %>
</body>
</html>